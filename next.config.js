/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // experimental: {
  //   runtime: 'experimental-edge',
  // },
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig
